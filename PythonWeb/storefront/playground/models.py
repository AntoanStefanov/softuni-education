from django.db import models

# Create your models here.
# Here we define the models classes for this app,
# We use model classes to pull out data from the database
# and present to the user.

# define your models - essentially, your database layout, with additional metadata.
# A model is the single, definitive source of information about your data.
# It contains the essential fields and behaviors of the data you’re storing.
