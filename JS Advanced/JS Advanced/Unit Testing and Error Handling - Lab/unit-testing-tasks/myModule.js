function sum(a, b) {
    return a + b;
}


function multiply(a, b) {
    return a * b;
}

module.exports = {
    sum, //sumFunc: sum, tuk kato kajesh samo funckiqta, to dava sushtoto ime kato kluch, sum: f sum()
    multiply
}